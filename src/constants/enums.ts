export enum ETokenType {
  ACCESS_TOKEN,
  REFRESH_TOKEN
}

export enum EUserRole {
  ADMIN = 'admin',
  USER = 'user'
}

export enum EUserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive'
}

export enum EStatus {
  NEW = 'New',
  IN_PROGRESS = 'In Progress',
  CLOSED = 'Closed'
}
